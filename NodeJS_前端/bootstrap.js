



exports.LoadUserDate = function()
{
  /*
    const fs=require('fs');
    const path = require("path");
    var content = '';
    let filename = path.resolve('./user.data');
    let filepath = path.resolve('./u.data');
    var c='write ok';

    fs.readFile(filename, 'utf8', function (err, data) {
        if (err) return console.log(err);
        content+=data;
      });

      console.log(content);

      var writeStram = fs.createWriteStream(filepath);
      writeStram.write(content);
      writeStram.end();
*/

}


